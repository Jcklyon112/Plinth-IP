import React from 'react'
import ReactDOM from 'react-dom/client'
import { RentEstimatePage } from './components/RentEstimatePage'

ReactDOM.createRoot(document.getElementById('root')!).render(
  <React.StrictMode>
    <RentEstimatePage />
  </React.StrictMode>,
)
